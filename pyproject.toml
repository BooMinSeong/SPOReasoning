[project]
name = "sporeasoning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "datasets",
    "peft",
    "vllm",
    "scikit-learn", # score.py 에서 직접 사용되지는 않지만, utils.utils 내에서 사용될 가능성 있음 (확인 필요)
    "pandas", # score.py 에서 직접 사용되지는 않지만, utils.utils 내에서 사용될 가능성 있음 (확인 필요)
    "numpy", # math_eval.py 에서 직접 사용되지는 않지만, sympy 등 하위 의존성에서 필요할 수 있음
    "sympy",
    "latex2sympy2",
    "word2number",
    "regex",
    'vllm',
    'deepspeed',
]
